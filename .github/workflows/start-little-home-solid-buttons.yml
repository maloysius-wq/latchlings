name: Start Little Home Solid Buttons

on:
  push:
    paths:
      - '.github/workflows/start-little-home-solid-buttons.yml'

permissions:
  contents: write

jobs:
  record:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Record current work
        run: |
          set -euo pipefail
          python3 .github/scripts/start_little_home_solid_buttons.py
          rm -f .github/workflows/start-little-home-solid-buttons.yml .github/scripts/start_little_home_solid_buttons.py
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add DEVELOPMENT_HANDOFF.md .github/workflows/start-little-home-solid-buttons.yml .github/scripts/start_little_home_solid_buttons.py
          git commit -m 'Start Little Home solid button refinement'
          git pull --rebase origin main
          git push origin HEAD:main
