function starSvg(on,cls=''){return `<span class="win-star ${on?'on':''} ${cls}"><svg viewBox="0 0 100 100" aria-hidden="true"><path class="core" d="M50 8 62 35 92 38 69 58 75 88 50 72 25 88 31 58 8 38 38 35 Z"/><path class="shine" d="M50 17 58 35 78 37 63 50 67 68 50 58 33 68 37 50 22 37 42 35 Z"/></svg></span>`}
winLevel=function(){const lev=LEVELS[currentLevel-1],stars=starsFor(lev);progress.stars[currentLevel]=Math.max(progress.stars[currentLevel]||0,stars);if(currentLevel<80)progress.unlocked=Math.max(progress.unlocked,currentLevel+1);saveProgress();const starRow=[1,2,3].map((n,i)=>starSvg(n<=stars,`s${i+1}`)).join('');modal(`<h2>Level cleared</h2><div class="win-stars" aria-label="${stars} stars earned">${starRow}</div><p>${movesUsed} moves. Best known solution: ${lev.optimal}.</p><div class="modal-actions"><button class="primary-small" id="nextLevelBtn">${currentLevel===80?'Finish':'Next Level'}</button><button class="secondary-small" id="replayBtn">Replay</button><button class="secondary-small" id="toLevelsBtn">Level Select</button></div>`);document.getElementById('nextLevelBtn').onclick=()=>{closeModal();if(currentLevel===80)screen('complete');else startLevel(currentLevel+1)};document.getElementById('replayBtn').onclick=()=>{closeModal();startLevel(currentLevel)};document.getElementById('toLevelsBtn').onclick=()=>{closeModal();chapterView=Math.ceil(currentLevel/10);screen('levels');renderChapter()}};